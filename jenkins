pipeline {
    agent any
    stages {
        stage('Build Project') {
            steps {
                // bat 'npm install'
                bat 'npm run build'
                bat "npm run rename ${BUILD_NUMBER}"
            }
        }

    }
}
